{
  "name": "fundraiseup-tech-assignment",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"**/*.ts\"",
    "start:app": "npm run build && node dist/app.js",
    "start:sync": "npm run build && node dist/sync.js",
    "start:fsync": "npm run build && node dist/sync.js --full_reindex"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^20.4.3",
    "prettier": "^3.0.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "dotenv": "^16.3.1",
    "mongodb": "^5.7.0"
  }
}
